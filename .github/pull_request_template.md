Closes #

<!-- What is true after this change that was not before, and why it was the right change. -->

## Tests and evidence

<!-- The commands you ran and what they printed. Each new test names the mutation that made it fail. -->

```
$ ./scripts/validate.sh
```

## Documentation

<!-- One line for every living document (AGENTS.md section 3). Generate the list for your diff:

       python3 tools/check-pr-docs.py --skeleton

     tick each line and finish the note: `updated: <what>` for a document this change edits, and
     `checked, no change: <what you looked for>` for one it does not. The `documentation` check
     fails when a living document is missing, a changed one is called unchanged, or an update is
     claimed that the diff does not contain. -->

## Out of scope

<!-- What you noticed and did not fix, each as a new issue. "None" if nothing. -->
