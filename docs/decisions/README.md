# Decisions

One file per decision that would otherwise be re-litigated or quietly reversed. A record
states what was decided, what the alternatives were, and what follows — not how something
works, which is the code's or the specification's job.

Numbering is sequential and permanent. A superseded record is marked superseded in place and
kept; its number is never reused.

| # | Decision |
|---|----------|
| [0001](0001-the-corpus-map-is-the-interface.md) | The corpus map is the interface, and it comes before the scaffolder |
| [0002](0002-boundary-policy-belongs-to-the-corpus.md) | Boundary policy belongs to the corpus, not to the repository |
| [0003](0003-a-phase-gate-names-a-rule-not-a-condition.md) | A phase gate names a rule, not a condition |
| [0004](0004-adapter-reach-is-a-property-of-the-entry.md) | Adapter reach is a property of the entry, recorded structurally |
| [0005](0005-a-field-earns-its-place-by-being-checkable.md) | A field earns its place by being checkable, not by naming a distinction |
| [0006](0006-the-general-rule-governs-entry-and-full-means-adversely-full.md) | The general rule governs entry, and "full" means adversely full |
| [0007](0007-a-conflict-is-a-question-not-a-pair.md) | A conflict is a question, not a pair |
| [0008](0008-recognising-a-delegated-standard-is-a-procedure-not-a-test.md) | Recognising a delegated standard is a procedure, not a test, and its residue is named |
| [0009](0009-absence-is-a-verdict-with-evidence.md) | Absence is a verdict with evidence, and scope belongs to a rule, never to a section |
| [0010](0010-whose-fact-it-is-does-not-decide-the-kind.md) | Whose fact it is does not decide the kind; the measure the corpus states does |
| [0011](0011-a-gate-has-a-direction.md) | A gate has a direction, and the field it sits in says which (supersedes 0003's polarity paragraph) |
| [0012](0012-a-fact-the-corpus-implies-is-a-derived-entry.md) | A fact the corpus implies and never states is a derived entry, and it cites nothing |
| [0013](0013-verification-posture-belongs-to-the-corpus.md) | How a corpus is verified, and whether a map may quote it, belong to the corpus too (extends 0002) |
| [0014](0014-a-map-is-checked-by-a-blind-second-mapping.md) | A map is checked by a blind second mapping, and every disagreement is resolved against the corpus before it is used |
| [0015](0015-a-map-is-published-as-a-versioned-package.md) | A map is published as a versioned package, checked before it can be a version, and overlaid by its engine on three fields |
| [0016](0016-a-map-package-is-data-not-code.md) | A map package is data, not code: the factory checks it with its own checker and never runs the package's (amends 0015) |
| [0017](0017-a-map-change-carries-a-review-of-its-bytes.md) | A map change carries a review of its exact bytes, and a check refuses one that does not (enforces 0014) |
| [0018](0018-every-file-the-factory-writes-has-one-owner.md) | Every file the factory writes has one owner: generated, managed (factory policy with a recipe version, hand edits refused) or engine-owned |
| [0019](0019-randomness-is-declared-by-the-corpus.md) | Whether an engine may draw random values is declared by the corpus: `randomness: none` or `seeded` (extends 0002 and 0013) |
| [0020](0020-a-section-citation-names-its-lead-in-and-a-section-map-lists-its-extent.md) | A section citation can name its lead-in, and a section-designation map lists its extent; the manifest is never inline, and `definedElsewhere` alone answers a pointer to an unadmitted corpus (extends 0009) |
| [0021](0021-a-gate-outside-the-slice-is-held-by-the-caller.md) | A gate outside the map's slice is a `scope: out` entry, and whether it holds is a fact the caller states and the engine never infers (extends 0003 and 0011) |
| [0022](0022-a-licensed-copy-is-used-locally-by-a-named-operator-and-never-published.md) | **Superseded by 0028, withdrawn.** A licensed `local-copy` corpus may be used locally by a named operator, under an explicit flag and an allowlisted `gh` identity, and a map of one is never published (extends 0013 and 0015) |
| [0023](0023-a-map-package-is-licensed-as-its-corpus-and-the-factory-are.md) | A map package is licensed as its corpus and the factory are: a `LICENCE.txt` it carries, the corpus terms restating the manifest's `licence`, Apache-2.0 for the rest (amends 0015) |
| [0024](0024-a-quote-is-of-the-extraction-and-a-page-extent-can-end-at-a-heading.md) | A quote is verbatim of the extraction the manifest names, an entry the extraction garbles declares its defect and rendered reading, and a page extent can end before a heading on its last page (extends 0004 and 0020) |
| [0025](0025-an-assertion-names-who-asserts-it-and-an-operation-names-what-it-draws.md) | An assertion names who asserts it (`assertedBy`), and an operation names what it draws (`draws`, `ambiguity.affectsDraws`), each anchored in the corpus's words (extends 0005, amends 0019) |
| [0026](0026-a-meaning-the-same-corpus-gives-elsewhere-is-an-entry-and-a-corpus-declares-its-pointers.md) | A meaning the same corpus gives outside the slice is a `scope: out` entry the rule names in `crossReferences` (and `dependsOn` where it modifies the rule), and each corpus declares its `pointerPhrases`; a silent zero fails (extends 0009, 0020 and 0021) |
| [0027](0027-an-owners-ruling-is-held-by-the-engine-and-checked-by-the-factory.md) | An owner's ruling on part of an unresolved question is held in the engine's overlay (`rulings`, with `declines` for the parts still declined, their spans covering the question), never in the map; the factory checks it against the map, generates `Rulings.g.cs` and records it in provenance (extends 0015, amends 0005 C) |
| [0028](0028-the-factory-admits-only-corpora-whose-licence-permits-publishing-them.md) | The factory admits only corpora whose licence permits committing and publishing their text and maps: public domain, or an open licence read from the manifest's `licence`, refused by intake and `pack-map.py` otherwise; `deckard` stays outside the factory, and #3's criterion 1 is a blind rebuild of `hoyle-backgammon` (supersedes 0022, and the licensed-only parts of 0013, 0015 and 0023) |
| [0029](0029-the-rails-are-emitted-by-default-and-vendor-choice-is-engine-owned-configuration.md) | The rails are emitted by default, `AGENTS.md` governs every agent, vendor and label choices live in engine-owned `.github/agent-policy.json`, and GitHub enforcement is applied by an explicit idempotent command (extends 0018 and 0001) |
| [0030](0030-a-repeated-passage-is-identified-by-the-container-its-citation-names.md) | A passage the corpus prints more than once is identified by the container its citation names, and in a page-marked corpus that container is the heading path; a citation the path does not resolve to one printing is refused, never guessed (extends 0024 and 0020) |
| [0031](0031-an-example-that-bounds-a-term-is-recorded-as-a-bound.md) | An example that is the corpus's only authority for a rule no operative sentence states is an entry; an example that bounds a term an operative rule leaves open is `ambiguity.bounds` on that rule, admitted only where the dimension is comparable, and an owner's ruling that contradicts one fails the engine's gate (extends 0005 and 0027) |
| [0032](0032-mapping-validation-and-generation-are-three-subsystems-over-one-contract.md) | Mapping, map validation and engine generation are three sibling subsystems over one map contract, which none of them may bypass and none may import another through; the direction is checked, and the mapper is not yet its own repository (extends 0001 and 0016) |
| [0033](0033-the-validator-is-the-adversary-and-validates-the-uncertainty-too.md) | `checkmap` is `mapvalidator`: the validator is the adversary, it challenges six kinds of truth, what it validates includes the uncertainty, and the three subsystems claim three different determinisms (extends 0032) |
| [0034](0034-a-valid-unresolved-state-is-established-not-asserted.md) | A valid unresolved state is established by records outside the entry, never asserted by it: the map gains no `readings` field, an adjudicated "the corpus does not settle it" must be recorded in the map as ambiguous (`superposition`), an open question returns a reason its own correspondence rows produce (`unresolved-reason`), and a bound names a term the question records as open (`bound-term-open`) (carries out 0033 §3, extends 0014) |
| [0035](0035-a-rule-stated-in-a-table-row-is-cited-by-its-row.md) | A rule stated in a table is cited by its row (`§ 172.101 table 3, row [column 2 = "Acetal"], column 7`), a row is named by `column = value` pairs that must resolve to exactly one row, the extraction of a row is its cells in column order with the empty ones kept, `table-row` is a unit, and an extent that slices a table names the rows it takes and accounts for every table it does not (extends 0020, 0024 and 0030) |
| [0036](0036-a-paragraph-inside-a-wrapper-takes-the-designation-the-wrapper-continues.md) | A paragraph the corpus prints inside a wrapper (`EXTRACT`, `NOTE`) takes the designation of the paragraph the wrapper continues, at any depth and with no new citation form; a wrapper that opens a division of the section or holds a `<P>` stating its own designation is **unplaced**, reported and cited by nothing; and a note takes the paragraph its own heading names (extends 0020, sibling of 0035) |
| [0037](0037-an-ellipsis-skips-whole-paragraphs-and-never-words-inside-one.md) | An ellipsis in a prose `evidence` may skip **whole paragraphs** and may never drop words from inside one: the text before each `...` ends where a paragraph ends and the text after it begins where one begins, the quote's own two ends stay exempt, and a table row carries no ellipsis at all (settles the question #18 left open, sibling of 0035) |
| [0038](0038-a-map-declares-the-passages-its-grammar-cannot-address.md) | A map declares, per corpus, each passage its citation grammar has no address for — `extent.unreachable`, naming the closed `reason` the walk gave and what reaching it `requires` — and the locator run holds the declaration to the corpus in both directions: an undeclared refusal fails, and a declaration the walk reaches fails. A positive statement of what could not be reached, not an exclusion from the extent, and not a reversal of 0036 (extends 0009 and 0020, bounded by 0036) |
| [0039](0039-the-manifest-pins-every-corpus-a-map-cites.md) | A map may cite several corpora; the manifest is the authority for which and for the bytes of each, and no per-corpus baseline is added to the map. The envelope's `corpus`/`baseline` stamps the principal corpus and claims nothing about the others. Intake independently re-hashes every cited corpus, the engine carries them all, and provenance records them as a canonical `corpora` list sorted by `sourceId` (`provenanceFormat` 5). A served-document boundary is a delivery artifact and must not split one mapping problem (extends 0013 and 0015, sibling of 0038) |
| [0040](0040-a-protocol-is-about-one-corpus-and-a-map-has-one-per-corpus-it-cites.md) | A mapping protocol is about one corpus, and a map has one per corpus it cites: `mapping-protocol.json` for a map citing one, `mapping-protocol-<sourceId>.json` once it cites several. A cited corpus with no protocol, a protocol for a corpus the map does not cite, and one protocol standing for two corpora are each refused — a union of two corpora's units and mechanisms could not be wrong about either in particular, and a declared interrogation nobody performs reads as coverage (extends 0039 and 0032) |
| [0041](0041-a-coded-pointer-is-made-by-the-column-it-sits-in.md) | A `coded-pointer` is a pointer token whose meaning is established by a structural context the corpus declares — for 49 CFR § 172.101, the column it sits in — and not by its text: the fourth `pointerMechanisms` member, declared with the existing `vocabularyFrom` plus a `column` parameter, detected by reading only entries whose citation names a cell in that column. Forced by measurement: `defined-term-use` detected all 33 of trial 10's column 7 codes and also read the numeric code `148` as a pointer in column 14's stowage provisions and in a 46 CFR part number. An `equations` modality was rejected — `illustrations: unsupported` plus the open `beyondAdapter.modality` already represents TP1's image-served formula (extends 0026 and 0035) |
| [0042](0042-the-mapper-walks-every-corpus-a-map-cites.md) | The mapper walks every corpus a map cites — each with its own adapter, its own share of the one declared extent, its own protocol and only the entries citing it — and the inventory and sweeps report per corpus, total, and the worst verdict of them. An extent section no cited corpus contains is refused of the union, so splitting the extent cannot lose the invariant. Forced by measurement: trial 10's map was either refused outright or silently measured over 104 of 716 units (extends 0039 and 0040, carries out 0009) |
| [0043](0043-a-row-blank-in-the-column-that-names-the-row-above-is-named-below-it.md) | A row the corpus leaves blank in a column an earlier row fills is addressed relative to that earlier row — `row blank in column 1 below row [column 1 = "IB2"]` — with ordinary `column = value` discriminators where the run holds more than one, and zero or two matches refusing as a row key already does. The column taken is the one whose anchor is nearest above and whose run is shortest, because column order alone produced an address that was unique, stable and named the wrong material. The locator identifies the passage and claims nothing about it continuing the anchor. Forced by § 172.102 table 2, where IB2's second row is byte-identical to IB1's and no key names either (extends 0035)
| [0044](0044-one-printed-code-can-name-more-than-one-rule.md) | A term in a corpus's vocabulary names one **or more** defining entries — two term-anchored `crossReferences`, no new field — and a pointer to a term with several is accounted for only when the entry names every one of them. Declaration order decides nothing, and the same target declared twice is reported rather than collapsed. Forced by § 172.102(c)(4), which gives `IB3` two rules in two tables: `vocabulary_of` kept the last declaration, discarded the first, and every check passed because both targets were real entries. All six committed maps produce byte-identical pointer reports (extends 0026 and 0041, sibling of 0043)
| [0045](0045-a-vocabulary-is-distributed-over-the-entries-that-define-its-terms.md) | A vocabulary is distributed over the entries that define its terms: an optional, evidence-anchored `defines` on each defining entry, and `coded-pointer` naming the `vocabulary` those declarations add up to rather than one entry that indexes them. A definition is anchored in the defining entry's evidence, a `crossReference` stays anchored in the pointing entry's, and neither implies the other. Forced by 49 CFR § 172.102, which prints its codes one per table row and has no passage listing them, so 0041's `vocabularyFrom` entry could only be written by inventing the list — which 0026's anchoring rule refuses, correctly. 0044's cardinality becomes the ordinary result of two entries defining one term (amends 0041, leaves 0026 and 0044 as they stand)
| [0046](0046-an-additional-rule-can-continue-a-definition.md) | A passage may continue the one vocabulary definition made by another entry through an explicit `continuesDefinition` relation whose structural anchor independently resolves the continuation passage under that defining passage. The continuation inherits the target's one direct `(vocabulary, term)`; it does not write either itself. Forced by § 172.102 table 2's blank-code `Additional Requirement` rows, so 0045's direct evidence anchor remains intact, 0043 remains structural only, and 0044 sees every additional defining rule (extends 0044 and 0045, leaves 0043 unchanged) |
| [0047](0047-a-mapping-discovered-corpus-boundary-amends-admission-without-rewriting-it.md) | A corpus boundary discovered during mapping is added as an additive `referenceAmendments` correction: Phase-1 `references` remain the historical admission record, consumers read their union, broad part references cover child sections, and a correction cannot admit a corpus or alter pinning/licensing facts (extends 0009) |
| [0048](0048-a-verified-map-package-binds-the-exact-artifacts-its-publish-gate-read.md) | A verified map package binds its map, manifest, packaged checker and every cited corpus to the exact bytes its publish gate verified; intake independently re-establishes the same relationship, and legacy unbound packages are refused (amends 0015, extends 0039) |\n
| [0049](0049-the-gate-can-say-which-of-its-checks-a-change-owes.md) | The gate keeps one definition of acceptable and gains a vocabulary for narrower ones: `tools/validate-repo.py` carries the steps under `--full`, `--changed --base <sha>` (tests and repository-wide checks always, only the map fixtures a diff could have broken) and `--release <map>` (every structural check, only the tagged map packed). Every rule is a table row a test covers, and everything the table cannot place — an unknown path, a non-fixture `examples/` directory, a missing base, an unreadable diff, a classifier that raises — widens to `full`, which is not itself computed and may not skip. Measured first: the map work it narrows is ~3s of a 3m05s run, so this is the vocabulary a release and a CI workflow need, not a wall-clock claim (extends 0033, bounded by 0015) |
| [0050](0050-a-pull-request-is-gated-on-what-its-own-diff-owes.md) | A pull request runs `validate-repo.py --changed --base <its own base>` and the classifier decides, so "force full for core tooling" is not copied into YAML where it would drift; the checkout is unshallowed, because a base that is not there makes the diff unreadable and widens silently. A push to `main` and a weekly schedule run `--full`, which is what makes the network check -- the README's prose against the state of the issues it cites -- reachable without a code change; it leaves `--changed`, while `check-readme-status.py` stays at every scope. The publish gate runs `--release <map>`: every structural check over every map, only the tagged map packed, and its two independent packs and their digest comparison untouched and now tested rather than merely commented. Corrects 0049's `Scope.engine`, which a packable map also owes (carries out 0049, bounded by 0015) |
| [0051](0051-every-evidence-artifact-says-which-check-reads-it.md) | Every tracked artifact under `examples/` is named in `tools/evidence-lock.json` with its SHA-256, the parts of CI that read it, and the role that follows -- `release` (the packer reads it), `active` (another gate step does), `archived` (nothing does) -- and the gate holds the tree to the lock in both directions on every run. The role is measured, by an audit hook recording every file opened and the tool that opened it, because a hand-written role is a claim about what a checker does. Two attributions decide whether the measurement means anything and both were wrong first: the lock's own hashing is excluded by call stack, and a read by the repository-wide link check is recorded as `links`, keeping the 884 KB whose only verification is that its links resolve visible as that. The 892 KB nothing reads stays in the repository -- 4.8% of what is tracked, against an external store, a fetch in the verification path and links a reviewer cannot follow offline -- and `fetch-evidence.py --verify` is built and tested so the decision is reversible with a measurement (extends 0017, sibling of 0049) |
| [0052](0052-the-rails-stay-in-the-engine-that-runs-on-them.md) | The rails stay copied into the engine as managed files. Measured: 204 KB of an engine, 15% of the larger of the two that exist and 26% of the smaller. A pinned rails distribution would make offline verification a download, coarsen provenance from 26 file digests to one package digest, lose per-file `--adopt`, and could not move the rails a tool reads from the tree anyway; reusable workflows address 6.3 KB of already-thin YAML, and centralising the 130 KB of policy Python behind it would have every engine run another repository's code with its own token, with nothing in `provenance.json` able to hash what ran. The 70 recorded byte-versions are the design's evidence, not its cost: each is a rails change that reached an engine without overwriting an edit. Names what would reopen it -- rails past 50% of an engine, measurable migration lag across live engines, or a rail that becomes a library -- and requires a one-engine compatibility pilot first (extends 0029 and 0018) |
| [0053](0053-a-review-verdict-binds-the-exact-packet-and-reviewed-commit.md) | A review packet is assembled from a detached snapshot of one exact PR head and carries a deterministic `*.review.json` identity binding that reviewed head/base, the human packet, every entry packet, and reviewed policy/provenance/map context. New verdicts require that identity, refuse a moved PR, and take their status context from the reviewed packet rather than the caller checkout; legacy statuses remain readable, while provider authentication remains explicitly out of scope (amends 0029) |
| [0054](0054-a-verification-context-is-declared-and-proved-never-exempted.md) | The local SDK override re-pins `global.json` around restore and the gate, and the gate hashes `global.json` against `provenance.json`, so one run both wrote those bytes and called them forged. The substitution is now **declared** to the gate — `FACTORY_SDK_OVERRIDE_RECORDED` names the recorded bytes, kept outside the engine — and **proved** rather than exempted: the declared original must hash to what the record hashes, and what is on disk must be exactly that original with its SDK version replaced. The tree under verification is therefore known, it differs by one field of one file, and every line of the run says which SDK it actually proved. A declaration is refused under `CI=true`, an undeclared re-pin fails as it always did, and exempting the file, forging the record, wrapping `dotnet`, adopting the re-pin in a standalone `verify` and withdrawing the override were each rejected (extends 0018) |
| [0055](0055-coverage-reports-how-much-of-the-extent-is-quoted-and-a-map-declares-the-floor.md) | `coverage` reports, in all three locator checkers, the fraction of the declared extent that verified evidence quotes — the union of the quotes' spans over the extent's own length — and a map may declare `extent.quoted`, the floor it claims it can show, which `coverage` holds it to. A map declaring none is measured, printed and not failed: across the committed maps the fraction runs from 19% (`hazmat-172-table`) to 100% (`faa-part-107`), so a threshold that passed the low end would fail nothing and one that passed the high end would refuse an honest map, and #270 asked for the decision rather than a number nobody argued for. Forced by measurement: `coverage` counts units touched and one quote reaches a page, so the harness's `drop-entry` is missed on 4 of 5 committed maps. A table row is not paragraph text and is not in the fraction: `extent.tables` accounts for a read table row by row, which is finer (extends 0009, sibling of 0020) |
| [0056](0056-a-wrapper-whose-heading-names-a-division-continues-nothing.md) | A wrapper whose own heading **names a division of the corpus** — `Appendix A to § 172.101—…`, `Subpart B—…` — is unplaced whatever it is printed after, a third signal for 0036's existing `division-wrapper` refusal. It supersedes one sentence of 0036, that the division test reads no words: a wrapper opening a division, titled `HD2` rather than `HD1` and printed directly after a designated paragraph, defeats both structural tests together and inherits that paragraph's designation, and it is structurally identical to § 172.102's six captioned provision runs, which must keep their address. Two structural third signals were measured against the corpus and each costs provisions 0036 exists to reach. What is read is an address the corpus states about itself, which is the reading 0036 part 4 already makes of a note's heading. The adapter now asks every placement test that needs no designator tree, including the example-head test it could always have asked, and the one test it cannot ask — `note-heading-elsewhere` — is bounded by a test over every corpus rather than described in prose. Neither `NOTE_HEAD` nor `EXAMPLE_HEAD` is widened, under #265's standard; each refusal instead quotes the words it could not read, and a note heading claiming an address this grammar cannot parse is unplaced rather than left to inherit its position (supersedes part of 0036, sibling of 0038) |
| [0057](0057-a-verdict-rests-on-entry-evidence-read-once-and-bound-to-the-reviewed-commit.md) | A review packet identity is written only when the entry evidence it names exists and was built from map bytes bound to the reviewed commit, and a verdict is recorded only from such an identity. `--package-map` is read **once** and the `entry-packet.py` subprocess is handed those exact bytes, closing the second open that let a file replaced between the check and the use produce entry packets from map B under map A's checked digest. Nothing reaches `--out` until every refusal has passed, so a refused packet creates no directory and writes no file. #372's three options were decided by measurement rather than split: the MSBuild-resolved path cannot be verified without a `dotnet restore` of a throwaway detached worktree per packet, and — because `RulesFactoryMap` comes from the restored package — that path today produces **no entry packet at all** while still writing a recordable identity, so the refusal costs nothing that worked. A packet naming no entry reads no map and is unaffected, and `--stdout` keeps the unbound path for reading, writing nothing that can authorize a verdict (extends 0053) |
| [0058](0058-a-pointer-is-declared-by-the-passage-that-makes-it-whatever-the-engine-does-with-the-rule.md) | A `crossReferences` declaration is owed by the passage that makes the pointer; `scope` and `status` do not bear on it. 0026 § 1 was written from the in-scope side and left open what a `scope: out`, `status: declined` entry owes, which `defined-term-use` made concrete by finding three undeclared namings in one map — all three out of scope and declined. The map answered it: two out-of-scope declined entries in it already declare, one of them for a bare naming with no pointer words, and `grappling-ends` declares for the Incapacitated it names and not for the Grappled it names twice. So the three are omissions, not a convention. The principle is 0026's own from the other side — this field records what the corpus does, `scope` and `status` what the engine does, and a decision about implementation may not silence a fact about the text. The detector therefore carries no scope filter, `validate.sh` stops accepting NOT VERIFIED on that step, and #208 closes with no new mechanism: what it was missing was this ruling, not a design. Satisfaction stays per term rather than per naming, bounded as #399 (extends 0026) |
| [0059](0059-a-missing-definition-dominates-an-open-question-and-a-reference-may-name-a-class.md) | An entry may carry `definedElsewhere` (or `beyondAdapter`) **and** an `ambiguity` block, and the correspondence table's row order says what it returns: a missing definition dominates an open question. The exclusion that forbade the pair was written before the table had an order, and cost a true fact — `method-of-allocation` defers a term to an unadmitted statute *and* leaves a gap of its own, and had to write the deferral as a cross-reference while `depreciation-not-excludable` declared the same statute for the same term the ordinary way. Trial 9's adjudication reached the same precedence one edge away — "`definedElsewhere` relocates the reason an entry declines; it never converts a decline into an answer" — so the rule for one entry and the rule across a `dependsOn` edge are one rule, now `check-map.py --only inherited-reason`. With the exclusion gone the two conventions for an unadmitted reference collapse to one: a passage fixing the meaning of a term the rule uses is `definedElsewhere`, a different rule it points at is `crossReferences[].unmapped`, a fact the caller reports is neither. And a reference may name a **class** of corpora — local law — declaring `class: true` with no `citation` and a `note`; the marker is explicit because an absent citation cannot carry it, `air-almanac` having none and being one publication (supersedes 0005 D's exclusion, extends 0020 §4 and 0026 §3) |
| [0060](0060-one-adjudication-record-one-shape-and-the-vocabulary-is-declared-in-the-file.md) | The blind second mapping's adjudication record is one file, `blind-mapping/resolutions.json`, one shape — an `about`, a `verdicts` legend, an `unsettledVerdict` naming the term that means *the corpus does not settle it*, and an `adjudications` list whose rows carry the verdict in a field — and each record declares the vocabulary its own verdicts are written in. `results.json` is the comparison and is no longer read as an adjudication. The two verdict vocabularies stay where they were used, because a verdict is evidence and a letter rewritten afterwards records something nobody wrote; what is unified is that the meaning is in the file rather than in a README, so `epistemic.py` carries one parser and no hardcoded terms, and a verdict a record's own legend does not define fails as a missing one does. `compare.py` rewrites each row's structural fields from the flags it raised and refuses to write when the envelope is not the shape. No map's bytes move, so no `review.json` and no re-review is owed under 0017; all three `results.json` reproduce byte-identically and every `superposition` verdict is unchanged (carries out 0034, extends 0014) |
| [0061](0061-an-injection-names-the-refusal-it-expects-and-a-catch-by-another-rule-is-a-different-outcome.md) | Every `tools/mutate-map.py` injection names the refusal it expects — a rule, a family of rules that answer one question, or `None` where no rule is written to catch the error and the mutation measures that gap — and a mutation refused by any other rule is recorded and reported as a distinct outcome rather than as a catch. The intended rule counts only where its own detector refused, and `byIntendedRule` joins the shape the gate holds the committed measurement to. The catch rate is unchanged at 28 of 62; what it could not say before is that 3 of those 28 say nothing about the rule they were counted for — `same-passage-evidence` has no rule at all and was caught once, incidentally, by `cross-references`, and `applicability-reach` accounts for one of the three `remove-applicability` catches, the others belonging to `superposition` and `gates` (extends 0034's measurement, closes #362) |
| [0062](0062-the-publish-gate-reads-the-version-it-replaces-from-a-tag-in-this-repository.md) | `tools/pack-map.py` passes `check-map.py --previous`, reading the predecessor from the highest `map/<name>/vX.Y.Z` tag below the version being packed and staging those bytes in the snapshot the publish checks already run against (0048). nuget.org was refused because a network read makes a gate's verdict depend on a remote service, and a publisher-named path proves whatever it was handed. A first publish passes no `--previous` and is not refused; a tag that exists and does not contain the map is a refusal; which version was compared, or that none was, is printed every run. Three of four maps now compare against a predecessor, and the fourth — `tax-121-principal-residence` at v2.0.0 with no v1 tag — reports that its predecessor is unreachable rather than calling itself a first publish (carries out 0015, closes #378) |
| [0063](0063-no-new-map-concept-without-a-corpus-that-forces-it.md) | No new map concept without a corpus that forces it: decomposition attempted, the failed representation exhibited, a second reading agreeing, and the check named (extends 0005) |
| [0064](0064-a-page-extent-can-start-after-a-heading-and-the-heading-joins-two-maps.md) | A page extent may name `startsAfter`, a heading on page `from` after which the slice begins — the mirror of 0024's `endsBefore`, checked by `extent-start` in the page-marked PDF text checker, by `extent` in `check-map.py`, and by the enumeration in `mapper inventory`. Forced by a corpus: the rest of *Playing the Game*, pp. 16–18, begins halfway down p. 16 where trial 7's combat map ends, and a whole-page `{from: 16, to: 18}` asserts it read a half-page another map read — an overclaim `coverage` and `extent-bounds` both pass, and whose only repair inside the contract was thirty rejections whose ground would be *another map read this*. The slice starts at the **end** of the named line, so the heading is in neither map's extent: one line per split, and it is the one unit every map already declines as `ground: heading` (extends 0024 § 3, whose "There is no `startsAfter`" it supersedes; admitted under 0063, whose second-reading step is recorded as having no analogue for an extent) |
| [0065](0065-bloodied-is-measured-against-the-hit-point-maximum.md) | *Bloodied* is `current Hit Points ≤ Hit Point maximum ÷ 2`. SRD 5.2.1 p. 16 says "half your Hit Points" and the same paragraph uses *your Hit Points* for the current total, on which the test holds only at 0; every other threshold in the chapter measures against the maximum. Recorded as `fate: decision` on `bloodied` rather than as `clear` with the choice buried in a `note` — which is what the map's first draft did — and not as `unresolved`, because the rival reading is not a rival reading of the rule but one on which the rule says nothing. Rounding is not decided and is not needed. Superseded by a later slice that states the threshold, rather than reaffirmed (a reading under 0005; adds no concept, so 0063 does not govern) |
| [0066](0066-the-cited-page-chooses-among-the-printings-the-heading-path-selects.md) | Where the heading path selects several printings of a quote, the **cited page** chooses among them — and only among them: it does not rescue a path that selected nothing, and it decides nothing where two of the selected printings are on that page. 0030 gave the path the whole of the identification and left the page to be asserted afterwards, which made the **earlier** of two identical printings permanently uncitable — a prefix before p. 5 is a prefix before p. 187 — and cost a real rule: SRD *Round Down* was in no map's scope, and the combat map held it by extending its span with a `{6}` marker, the practice 0030 itself retired. A locator here is a heading path *and* a printed page, and both facts are printed on the page a reader turns to; an ordinal, which 0030 rejected, is neither. `srd-52-combat`'s span loses the marker under a `non-semantic` exemption, `srd-52-playing-the-game` gains `round-down` in scope and its inventory loses the `restatement` rejection that was never the true reason, and every run says when the page did the work (extends 0030; adds no map concept, so 0063 exempts it as a repair) |
| [0067](0067-a-composition-is-a-union-with-a-namespace-and-what-it-merges-it-derives.md) | Several map packages compose into one document: compatibility is that they are readings of one ruleset over one corpus (five refusals, each with its own message); identity is `(package, entry id)` — `Srd52Combat.round-down`, which is one path segment for `overlay/<id>.json` and pascals to a C# member — with every reference inside that package moved with it, and **a single package is not namespaced**, so no existing engine churns; and a declined entry of one package is **superseded** by an in-scope entry of another when the span their evidence occupies in the corpus contains it. The span, because text alone merges the two printings of *Round Down* (the thing 0030 exists to stop) and the citation alone misses one passage two mappers gave different heading paths. A quote the corpus repeats has no span, so the composition reports it and merges nothing — over the three SRD maps, 8 supersessions and the two `round-down` stubs left alone. The composed document declares no extent: two page ranges are not one, and `--phase consumer` reads none (extends 0039 and 0042; adds no map concept, so 0063 does not govern it) |
